import { createSession, makePassword } from "../../../lib/auth";
import { mutate } from "../../../lib/store";
export async function POST(req:Request){const body=await req.json(),setupToken=process.env.NINE_T_SETUP_TOKEN;if(setupToken&&body.setupToken!==setupToken)return Response.json({error:"The setup key is incorrect."},{status:403});if(typeof body.username!=="string"||body.username.length<2||typeof body.password!=="string"||body.password.length<8)return Response.json({error:"Use a username and a password of at least 8 characters."},{status:400});let exists=false;await mutate(d=>{if(d.config.initialized){exists=true;return}d.user={username:body.username.trim(),...makePassword(body.password)};d.config.initialized=true;d.config.modules={...d.config.modules,...body.modules};d.config.exposure=body.exposure||"public"});if(exists)return Response.json({error:"9t is already configured."},{status:409});await createSession();return Response.json({ok:true})}
