import { randomUUID } from "crypto";
import { writeFile } from "fs/promises";
import { authenticated, unauthorized } from "../../../lib/auth";
import { addObject, readData, uploadDir } from "../../../lib/store";
export const dynamic="force-dynamic";
export async function GET(req:Request){if(!await authenticated())return unauthorized();const d=await readData(),url=new URL(req.url),trash=url.searchParams.get("trash")==="true";return Response.json({objects:d.objects.filter(o=>trash?!!o.deletedAt:!o.deletedAt),config:d.config})}
export async function POST(req:Request){if(!await authenticated())return unauthorized();const form=await req.formData(),type=String(form.get("type")),name=String(form.get("name")||"").trim();if(!name||!["snippet","file","link"].includes(type))return Response.json({error:"A valid type and name are required."},{status:400});if(type==="file"){const file=form.get("file");if(!(file instanceof File))return Response.json({error:"Choose a file."},{status:400});const d=await readData();if(file.size>d.config.maxSizeMb*1024*1024)return Response.json({error:`File exceeds ${d.config.maxSizeMb} MB.`},{status:413});const storageKey=randomUUID();await writeFile(`${uploadDir}/${storageKey}`,Buffer.from(await file.arrayBuffer()));return Response.json(await addObject({type:"file",name,mimeType:file.type||"application/octet-stream",sizeBytes:file.size,storageKey}))}if(type==="link"){let link=String(form.get("url")||"");try{link=new URL(link).toString()}catch{return Response.json({error:"Enter a valid URL."},{status:400})}return Response.json(await addObject({type:"link",name,url:link}))}return Response.json(await addObject({type:"snippet",name,content:String(form.get("content")||""),language:String(form.get("language")||"text")}))}
