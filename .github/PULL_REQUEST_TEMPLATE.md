## What

## Checks

- [ ] `node --test tests/*.test.mjs` green
- [ ] `npm run typecheck` clean
- [ ] Docs updated (`docs/`, and `site/` if the public site is affected)
- [ ] No user-data paths touched (`data/`, `.env.production`, `backups/`)

## Notes
